## What this changes

<!-- The behaviour that is different afterwards, not the files touched. -->

## Why

<!-- The problem it solves. If it fixes an issue, link it. -->

## How it was checked

<!-- `pnpm test`, `pnpm lint`, `pnpm typecheck` and `pnpm build` all pass for
     anything that ships. Say what you ran beyond that — a real turn against a
     harness, a packaged build, a screenshot of the surface you changed. -->

- [ ] `pnpm test`
- [ ] `pnpm lint` and `pnpm typecheck`
- [ ] `pnpm build`
- [ ] Tried it in the running app
